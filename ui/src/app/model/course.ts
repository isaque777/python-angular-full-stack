export interface CourseData {
  University: string;
  City: string;
  Country: string;
  CourseName: string;
  CourseDescription: string;
  Currency: string;
  StartDate: string; // Use string for date in ISO format
  EndDate: string;   // Use string for date in ISO format
  Price: number;     // Use number for floating-point values
}


